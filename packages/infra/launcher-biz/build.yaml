targets:
  $default:
    builders:
      # 1. 禁用默认的 drift_dev 构建器
      drift_dev:
        enabled: false

      # 2. 启用模块化构建器
      drift_dev:modular:
        enabled: true

      # 3. 启用分析器构建器 (必须与 modular 同时开启)
      drift_dev:analyzer:
        enabled: true